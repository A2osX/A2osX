NEW
  AUTO 3,1
*/--------------------------------------
* # Online
* Get ProDOS Volume Info
* ## C
* `#include <prodos.h>`
* `int online(short int volid, void *buf);`
* ## ASM
* `>PUSHB volid`
* `>PUSHW buf`
* `>LIBC Online`
* ## RETURN VALUE
*\--------------------------------------
K.Online		ldy #2
				jsr RAMSW.GetStkY
				sta K.MLI.PARAMS+1

				>LDYAI K.IOBuf
				>STYA K.MLI.PARAMS+2

				>MLICALL MLI.ONLINE
				bcs .9

				ldy #0
				jsr RAMSW.GetStkYW
				>STYA ZPPtr1

				sta IO.SETWRITEAUX

				ldy K.MLI.PARAMS+1
				beq .1

				ldy #16

.1				dey
				lda K.IOBuf,y
				sta (ZPPtr1),y
				tya
				bne .1

				sta IO.CLRWRITEAUX

*				clc

.9				rts
*/--------------------------------------
* # ChTyp
* Change Type of a ProDOS File
* ## C
* `#include <prodos.h>`
* `int chtyp(const char *filepath, short int filetype);`
* ## ASM
* `>PUSHW filepath`
* `>PUSHB filetype`
* `>LIBC ChTyp`
* ## RETURN VALUE
*\--------------------------------------
K.ChTyp			jsr PFT.GetPath1
				bcs .99

				jsr PFT.Translate
				bcs .99

				bvs .98

				txa
				bne .98

				jmp FS.ChTyp

.98				lda #MLI.E.INVPATH
				sec
.99				rts
*/--------------------------------------
* # ChAux
* Change AuxType of a ProDOS File
* ## C
* `#include <prodos.h>`
* `int chaux(const char *filepath, int auxtype);`
* ## ASM
* `>PUSHW filepath`
* `>PUSHW auxtype`
* `>LIBC ChAux`
* ## RETURN VALUE
*\--------------------------------------
K.ChAux			jsr PFT.GetPath2
				bcs .99

				jsr PFT.Translate
				bcs .99

				bvs .98

				txa
				bne .98

				jmp FS.ChAux

.98				lda #MLI.E.INVPATH
				sec
.99				rts
*/--------------------------------------
* # ChAttr
* Change Attributes of a ProDOS File
* ## C
* `#include <prodos.h>`
* `int chattr(const char *filepath, short int attributes);`
* ## ASM
* `>PUSHW filepath`
* `>PUSHB attributes`
* `>LIBC chattr`
* ## RETURN VALUE
*\--------------------------------------
K.ChAttr		jsr PFT.GetPath1
				bcs .99

				jsr PFT.Translate
				bcs .99

				bvs .98

				txa
				bne .98

				jmp FS.ChAttr

.98				lda #MLI.E.INVPATH
				sec
.99				rts
*--------------------------------------
PRODOS			cld
				jmp (.1,x)

.1				.DA PRODOS.StatDevA
				.DA PRODOS.Stat

				.DA PRODOS.OpenDir
				.DA PRODOS.ReadDir
				.DA PRODOS.CloseDir

				.DA PRODOS.Open

				.DA PRODOS.Close
				.DA PRODOS.Read
				.DA PRODOS.Write
				.DA PRODOS.LSeek

				.DA PRODOS.ChOwn
				.DA PRODOS.ChMod
				.DA PRODOS.MKDir

				.DA PRODOS.Remove
				.DA PRODOS.Rename

				.DA PRODOS.ChTyp
				.DA PRODOS.ChAux
				.DA PRODOS.ChAttr

				.DA PRODOS.FStat
*--------------------------------------
PRODOS.StatDevA
				bra *

*--------------------------------------
PRODOS.Open		>MLICALL MLI.GFINFO
				bcs .10

				lda IO.Type				Already Exists
				beq .2

				cmp K.MLI.PARAMS+S.FI.T
				beq .2

				lda #MLI.E.INCFF
				sec
				rts

.10				bit IO.Flags			Create if not exists ?
				bpl .9					No, return MLI error

				ldx #2					Yes, Create...

.1				lda IO.Type,x
				sta K.MLI.PARAMS+4,x	File type,Aux type
				dex
				bpl .1

				lda #S.FI.ST.STD
				jsr PRODOS.MLICREATE
				bcs .9

.2				jsr PRODOS.MLIOPEN
				bcs PRODOS.OpenE

				lda IO.Flags
				bit #O.WRONLY
				beq .5					Write mode ?

				and #O.APPEND			Append ?
				bne .3					yes, go to end of file

				stz K.MLI.PARAMS+2
				stz K.MLI.PARAMS+3
				stz K.MLI.PARAMS+4
				>MLICALL MLI.SETEOF		no, reset size to 0
				bra .4

.3				>MLICALL MLI.GETEOF
				bcs PRODOS.OpenE

				>MLICALL MLI.SETMARK
.4				bcs PRODOS.OpenE

.5				lda IO.Flags
				and #O.TEXT				Text Mode ?
				beq .8

				lda #$FF
				sta K.MLI.PARAMS+2
				lda #C.CR
				sta K.MLI.PARAMS+3
				>MLICALL MLI.NEWLINE
				bcs PRODOS.OpenE

.8
*				clc
.9				rts
*--------------------------------------
PRODOS.OpenE	pha						Save Error Code
				jsr PRODOS.Close
				pla
				sec
				rts
*--------------------------------------
PRODOS.OpenDir	>MLICALL MLI.GFINFO
				bcs .9

				lda K.MLI.PARAMS+S.FI.T
				cmp #S.FI.T.DIR
				bne .99

				jsr PRODOS.MLIOPEN
				bcs PRODOS.OpenE

*				clc

				rts

.99				lda #MLI.E.UNSUPST
				sec
.9				rts
*--------------------------------------
PRODOS.ReadDir 	ldy #S.FD.REG.REF
				lda (pFD),y
				sta K.MLI.PARAMS+1

				stz K.MLI.PARAMS+2
				lda /K.Buf512
				sta K.MLI.PARAMS+3

				stz K.MLI.PARAMS+4
				lda /512
				sta K.MLI.PARAMS+5

				>MLICALL MLI.READ		Read A block from directory
				bcs .99

				ldy #S.FD.DIR.EL		Check if first run....
				lda (pFD),y
				bne .2					no, we have all we need....

				ldx #3

.1				lda K.Buf512+$23,x		Get K.ReadDir.EL....	From Block
				sta K.ReadDir.EL,x
				dex
				bpl .1

				ldx #5

				ldy #S.FD.DIR.UID+5

.20				lda K.Buf512+$16,x
				cpy #S.FD.DIR.MODE+1
				bne .30

				and #$0F

.30				sta (pFD),y
				dey
				dex
				bpl .20

				inc K.ReadDir.EC		Add one to EC because we include VOL/DIR Header
				bne .4

				inc K.ReadDir.EC+1		...and ProDOS does NOT include header entry in EC
				bra .4

.99				rts
*--------------------------------------
.2				ldy #S.FD.DIR.EL+3
				ldx #3

.3				lda (pFD),y				get this session parameters from S.DIR
				sta K.ReadDir.EL,x
				dey
				dex
				bpl .3

.4				lda K.ReadDir.EC
				eor #$ff
				tax

				lda K.ReadDir.EC+1
				eor #$ff
				tay

				inx						Any entry remaining ?
				bne .5

				iny
				bne .5

				lda #MLI.E.EOF
				sec
				rts
*--------------------------------------
* pass #1 compute BufSize...
*--------------------------------------
.5				lda K.ReadDir.EPB
				sta K.ReadDir.ECIB

				lda #1					For Ending 0
				sta K.ReadDir.BufS
				stz K.ReadDir.BufS+1

				jsr K.ReadDir.P3Init

PRODOS.ReadDir1	lda (ZPPtr3)
				beq .7					Free slot....goto next

				and #$F0				get storage_type
				cmp #$F0				Volume header ?
				beq .1

				cmp #$E0				directory header ?
				beq .2

				lda (ZPPtr3)			regular DIR or File, get LEN
				and #$F					compute filename len
				sec						add "#<filename>" + S.STAT
				adc #S.STAT
				.HS 2C					bit abs
.1				lda #2+S.STAT			add "1." string + S.STAT
				.HS 2C					bit abs
.2				lda #2+S.STAT+3+S.STAT	add "1." + S.STAT + "2.." + S.STAT...

				clc
				adc K.ReadDir.BufS
				sta K.ReadDir.BufS
				bcc .3

				inc K.ReadDir.BufS+1

.3				inx						X,Y = !Total entry Count
				bne .7

				iny
				beq PRODOS.ReadDir2		we reached last entry in whole DIR

.7				jsr K.ReadDir.P3Next
				bne PRODOS.ReadDir1
*--------------------------------------
* pass #2 Fill Buffer...(ZPPtr1 & Ptr2 can now be trashed)
*--------------------------------------
PRODOS.ReadDir2	lda K.ReadDir.EPB
				sta K.ReadDir.ECIB

				>LDYA K.ReadDir.BufS	Get BufSize bytes buffer for storing results
				jsr K.ReadDir.GetBuf
				bcs .9

				jsr K.ReadDir.P3Init

.1				lda (ZPPtr3)
				beq .7					Empty slot, skip

				and #$F0
				cmp #$F0				Vol Header ?
				bne .2

				jsr K.ReadDir.ADD.
				bra .4

.2				cmp #$E0				Dir Header ?
				bne .3

				jsr K.ReadDir.ADD.
				jsr K.ReadDir.AddStatACL

				jsr K.ReadDir.ADD..
				bra .4

.3				jsr K.ReadDir.AddFNToBuf

.4				jsr K.ReadDir.AddStatACL

				lda K.ReadDir.EC			decrease global counter...
				sec
				sbc #1
				sta K.ReadDir.EC
				lda K.ReadDir.EC+1
				sbc #0
				sta K.ReadDir.EC+1
				ora K.ReadDir.EC
				beq .8						0! no more files in DIR

.7				jsr K.ReadDir.P3Next
				bne .1

.8				ldx #3
				ldy #S.FD.DIR.EL+3

.81				lda K.ReadDir.EL,x		Store back this session parameters to S.DIR
				sta (pFD),y
				dey
				dex
				bpl .81

				>LDYA K.ReadDir.pBuf
				clc
.9				rts
*--------------------------------------
PRODOS.CloseDir
*--------------------------------------
PRODOS.Close	ldy #S.FD.REG.REF
				lda (pFD),y
				sta K.MLI.PARAMS+1
				>MLICALL MLI.CLOSE
				bcs .9

				ldy #S.FD.REG.IOBUF+1
				lda (pFD),y

*				clc

				beq .9

				ldy #0					IOBUF is page aligned

.8				jmp MEM.FreeYA

.9				rts
*--------------------------------------
PRODOS.Read		ldx #MLI.READ

				.HS 2C					BIT ABS
*--------------------------------------
PRODOS.Write	ldx #MLI.WRITE

				stx .8

				ldy #2
				jsr RAMSW.GetStkYW
				>STYA K.MLI.PARAMS+2	buf

				ldy #0
				jsr RAMSW.GetStkYW
				>STYA K.MLI.PARAMS+4	count

				ldy #S.FD.REG.REF
				lda (pFD),y
				sta K.MLI.PARAMS+1

				lda #4					Param Count = 4 for MLIREAD & MLIWRITE
				sta K.MLI.PARAMS

				lda A2osX.IOOpt			AUX ?
				sta MLI.IOBANK

				jsr MLI

.8				.BS 1					SELF MODIFIED
				.DA K.MLI.PARAMS

				stz MLI.IOBANK

				bcs .9

				>LDYA K.MLI.PARAMS+6

.9				rts
*--------------------------------------
PRODOS.LSeek	ldy #S.FD.REG.REF
				lda (pFD),y
				sta K.MLI.PARAMS+1

				ldy #2
				jsr RAMSW.GetStkYW
				>STYA ACC32				offset

				ldy #4
				jsr RAMSW.GetStkYW
				>STYA ACC32+2

				jsr RAMSW.GetStk		whence
				cmp #SEEK.END
				beq .30

				bcs .98

				dec
				beq .20

				stz K.MLI.PARAMS+2
				stz K.MLI.PARAMS+3
				stz K.MLI.PARAMS+4
				bra .8

* SEEK.CUR
.20				>MLICALL MLI.GETMARK
				bcc .8
				rts

* SEEK.END
.30				>MLICALL MLI.GETEOF
				bcs .9

.8				ldy #0

				clc

.81				lda K.MLI.PARAMS+2,y
				adc ACC32,y
				sta K.MLI.PARAMS+2,y

				iny
				tya					3 bytes, 24 bits!!!
				eor #3
				bne .81

				bcs .99					Offset out of range!

.82				>MLICALL MLI.SETMARK
				bcc .9

				cmp #MLI.E.BEYEOF
				bne .9

				>MLICALL MLI.SETEOF
				bcc .82

.9				rts

.98				lda #E.BADARG
				.HS 2C					bit abs
.99				lda #E.FTB
*				sec
				rts
*--------------------------------------
PRODOS.ChOwn	stz K.MLI.PARAMS+3+S.FIEX.ACL		read mode

				>MLICALL MLI.ACL
				bcs .9

				ldy #1
				jsr RAMSW.GetStkY
				bmi .1

				sta K.MLI.PARAMS+3+S.FIEX.ACL.GID+1
				jsr RAMSW.GetStk
				sta K.MLI.PARAMS+3+S.FIEX.ACL.GID

.1				ldy #3
				jsr RAMSW.GetStkY
				bmi .2

				sta K.MLI.PARAMS+3+S.FIEX.ACL.UID+1
				dey
				jsr RAMSW.GetStkY
				sta K.MLI.PARAMS+3+S.FIEX.ACL.UID

.2				lda #$57							write mode
				sta K.MLI.PARAMS+3+S.FIEX.ACL

				>MLICALL MLI.ACL

.9				rts
*--------------------------------------
PRODOS.ChMod	ldy #0
				jsr RAMSW.GetStkYW
				>STYA K.S.STAT+S.STAT.MODE

				stz K.MLI.PARAMS+3+S.FIEX.ACL		read mode

				>MLICALL MLI.ACL
				bcs .9

				>LDYA K.S.STAT+S.STAT.MODE
				>STYA K.MLI.PARAMS+3+S.FIEX.ACL.MOD

				lda #$57							write mode
				sta K.MLI.PARAMS+3+S.FIEX.ACL

				>MLICALL MLI.ACL

.9				rts
*--------------------------------------
PRODOS.ChTyp	>MLICALL MLI.GFINFO
				bcs .9

				ldy #0
				jsr RAMSW.GetStkY

				sta K.MLI.PARAMS+S.FI.T

				>MLICALL MLI.SFINFO

.9				rts
*--------------------------------------
PRODOS.ChAux	>MLICALL MLI.GFINFO
				bcs .9

				ldy #0
				jsr RAMSW.GetStkYW

				>STYA K.MLI.PARAMS+S.FI.AUXTYPE

				>MLICALL MLI.SFINFO

.9				rts
*--------------------------------------
PRODOS.ChAttr	>MLICALL MLI.GFINFO
				bcs .9

				ldy #0
				jsr RAMSW.GetStkY

				sta K.MLI.PARAMS+S.FI.A

				>MLICALL MLI.SFINFO

.9				rts
*--------------------------------------
PRODOS.Remove	>MLICALL MLI.DESTROY
				rts
*--------------------------------------
PRODOS.Rename

				bra *
*--------------------------------------
PRODOS.EOF		>MLICALL MLI.GETMARK
				bcs .9

				ldy #2

.1				lda K.MLI.PARAMS+2,y
				sta ACC32,y
				dey
				bpl .1

				>MLICALL MLI.GETEOF
				bcs .9

				ldy #2

.2				lda K.MLI.PARAMS+2,y
				eor ACC32,y
				bne .8

				dey
				bpl .2

*				lda #0
				ldy #1					TRUE
*				clc
				rts

.8				ldy #0					FALSE
				tya
*				clc
.9				rts
*--------------------------------------
PRODOS.MkDir	lda #S.FI.T.DIR
				sta K.MLI.PARAMS+4
				stz K.MLI.PARAMS+5		Aux Type
				stz K.MLI.PARAMS+6
				lda #S.FI.ST.DIR
*--------------------------------------
PRODOS.MLICREATE
				sta K.MLI.PARAMS+7		Storage Type
				ldx #3

.1				lda MLI.DATE,x
				sta K.MLI.PARAMS+8,x	Create Date/Time
				dex
				bpl .1

				lda #S.FI.A.FULL
				sta K.MLI.PARAMS+3		Access
				>MLICALL MLI.CREATE
				rts
*--------------------------------------
PRODOS.MLIOPEN	>LDYAI 1024				get a ProDOS IOBUF
				ldx #S.MEM.F.ALIGN
				jsr MEM.Malloc.YAX
				bcs PRODOS.RTS

				>STYA K.MLI.PARAMS+3	Save Ptr to IOBUF for MLIOPEN call
				ldy #S.FD.REG.IOBUF+1
				sta (pFD),y
				dey
				lda #0
				sta (pFD),y

				>MLICALL MLI.OPEN
				bcs PRODOS.RTS

				lda K.MLI.PARAMS+5		get ref_num
				ldy #S.FD.REG.REF
				sta (pFD),y

				sta K.MLI.PARAMS+1		Next MLI Calls are REF_NUM based

*				clc

PRODOS.RTS		rts
*--------------------------------------
PRODOS.Stat203	>MLICALL MLI.GFINFO
				bcs PRODOS.RTS

				jsr X.ClrStat

				lda K.MLI.PARAMS+S.FI.T
				cmp #S.FI.T.DIR
				bne .1

				lda /S.STAT.MODE.DIR
				sta K.S.STAT+S.STAT.MODE+1

.1				lda K.MLI.PARAMS+S.FI.A
				sta K.S.STAT+S.STAT.P.ACCESS

				ldx #PRODOS.GFI.L

.3				ldy PRODOS.GFI-1,x
				lda K.MLI.PARAMS,y
				ldy PRODOS.Stat.Dst-1,x
				sta K.S.STAT,y
				dex
				bne .3

				>LDYAI K.MLI.PARAMS+S.FI.CDATE
				ldx #K.S.STAT+S.STAT.CTIME
				jsr PRODOS.PTime2StatYAX

				>LDYAI K.MLI.PARAMS+S.FI.MDATE
				ldx #K.S.STAT+S.STAT.MTIME
				jmp PRODOS.PTime2StatYAX
*--------------------------------------
PRODOS.FStat	lda pNames,y
				sta .2+1
				lda pNames+1,y
				sta .2+2

				ldy #$ff

.1				iny

.2				lda $FFFF,y				SELF MODIFIED
				sta K.PathBuf,y
				bne .1
*--------------------------------------
PRODOS.Stat		>MLICALL MLI.GFINFOEX
				bcc .1

				cmp #MLI.E.BADCALL
				beq PRODOS.Stat203		Apple WS CARD

				sec
				rts

.1				>LDYAI K.MLI.PARAMS+3
				>STYA ZPPtr3
*--------------------------------------
PRODOS.DirEnt2Stat
				jsr X.ClrStat

				ldx #PRODOS.DirEnt.L

.1				ldy PRODOS.DirEnt-1,x
				lda (ZPPtr3),y
				ldy PRODOS.Stat.Dst-1,x
				sta K.S.STAT,y
				dex
				bne .1

				ldy #S.FIEX.ACCESS
				lda (ZPPtr3),y
				sta K.S.STAT+S.STAT.P.ACCESS

				lda (ZPPtr3)
				and #$F0
				cmp #$D0					Dir Hdr or Dir ?
				bcc .3

				cmp #$F0					Vol Hdr ?
				bne .2

				stz K.S.STAT+S.STAT.SIZE

				ldy #S.FIEX.AUXTYPE
				lda (ZPPtr3),y
				sta K.S.STAT+S.STAT.BLOCKS
				asl
				sta K.S.STAT+S.STAT.SIZE+1

				iny

				lda (ZPPtr3),y
				sta K.S.STAT+S.STAT.BLOCKS+1
				rol
				sta K.S.STAT+S.STAT.SIZE+2

				lda #0
				rol K.S.STAT+S.STAT.SIZE+3

.2				lda /S.STAT.MODE.DIR
				tsb K.S.STAT+S.STAT.MODE+1

.3				ldy #S.FIEX.ACL
				lda (ZPPtr3),y
				cmp #$57
				bne .7

				iny
				lda (ZPPtr3),y
				bne .7

				ldy #S.FIEX.ACL.UID
				lda (ZPPtr3),y
				sta K.S.STAT+S.STAT.UID

				ldy #S.FIEX.ACL.GID
				lda (ZPPtr3),y
				sta K.S.STAT+S.STAT.GID

				ldy #S.FIEX.ACL.MOD
				lda (ZPPtr3),y
				tsb K.S.STAT+S.STAT.MODE
				iny
				lda (ZPPtr3),y
				tsb K.S.STAT+S.STAT.MODE+1

.7				lda #S.FIEX.CTIME
				ldx #K.S.STAT+S.STAT.CTIME
				jsr .8

				lda #S.FIEX.MTIME
				ldx #K.S.STAT+S.STAT.MTIME

.8				clc
				adc ZPPtr3
				tay
				lda ZPPtr3+1
				adc #0
*--------------------------------------
PRODOS.PTime2StatYAX
				>STYA TXTPTR

				ldy #3

.1				lda (TXTPTR),Y
				sta TIMEX.PT,y
				dey
				bpl .1

				stx FORPNT
				lda /K.S.STAT
				sta FORPNT+1

				ldx #X_PT2ST
				jsr CORE.JMPX
				bcs .9

				ldy #S.TIME-1

.2				lda TIMEX.ST,y
				sta (FORPNT),y
				dey
				bpl .2

.9				clc						ignore bad date error

				rts
*--------------------------------------
PRODOS.GFI		.DA #S.FI.T,#S.FI.AUXTYPE,#S.FI.AUXTYPE+1
				.DA #S.FI.UBLKS,#S.FI.UBLKS+1
PRODOS.GFI.L	.EQ *-PRODOS.GFI
*--------------------------------------
PRODOS.DirEnt	.DA #S.FIEX.TYPE,#S.FIEX.AUXTYPE,#S.FIEX.AUXTYPE+1
				.DA #S.FIEX.BLKUSED,#S.FIEX.BLKUSED+1
				.DA #S.FIEX.EOF,#S.FIEX.EOF+1,#S.FIEX.EOF+2
PRODOS.DirEnt.L	.EQ *-PRODOS.DirEnt
*--------------------------------------
PRODOS.Stat.Dst	.DA #S.STAT.P.TYPE,#S.STAT.P.AUXTYPE,#S.STAT.P.AUXTYPE+1
				.DA #S.STAT.BLOCKS,#S.STAT.BLOCKS+1
				.DA #S.STAT.SIZE,#S.STAT.SIZE+1,#S.STAT.SIZE+2
*--------------------------------------
PRODOS.MLICall	stx .1
				sta K.MLI.PARAMS

				jsr MLI

.1				.BS 1					SELF MODIFIED
				.DA K.MLI.PARAMS

				stz MLI.IOBANK			RESET ProDOS.FX to R/W to/from MAIN MEM

				rts
*--------------------------------------
*PRODOS.AtkCall	jsr MLI
*
*				.DA #MLI.ATALK
*				.DA K.MLI.PARAMS
*
*				rts
*--------------------------------------
MAN
SAVE usr/src/sys/kernel.s.prodos
LOAD usr/src/sys/kernel.s
ASM
