NEW
  AUTO 3,1
*/--------------------------------------
* # GetPWUID
* ## C
* `#include <pwd.h>`
* ` struct passwd *getpwuid(uid_t uid);`
* ## ASM
* `>LDYA uid`
* `>LIBC getpwuid`
* ## RETURN VALUE
*\--------------------------------------
K.GetPWUID		>STYA ZPPtr1

.99				lda #E.IUSR
				sec
				rts
*/--------------------------------------
* # GetPWName
* ## C
* `#include <pwd.h>`
* `struct passwd *getpwnam(const char *name);`
* ## ASM
* `>LDYA name`
* `>LIBC getpwname`
* ## RETURN VALUE
*\--------------------------------------
K.GetPWName		>STYA ZPPtr1

.99				lda #E.IUSR
				sec
				rts
*/--------------------------------------
* # GetGRGID
* ## C
* `#include <grp.h>`
* `struct group *getgrgid(gid_t gid);`
* ## ASM
* `>LDYA gid`
* `>LIBC getgrgid`
* ## RETURN VALUE
*\--------------------------------------
K.GetGRGID		

.99				lda #E.IUSR
				sec
				rts
*/--------------------------------------
* # GetGRName
* ## C
* `#include <grp.h>`
* `struct group *getgrnam(const char *name);`
* ## ASM
* `>LDYA name`
* `>LIBC getgrname`
* ## RETURN VALUE
*\--------------------------------------
K.GetGRName		

.99				lda #E.IUSR
				sec
				rts
*--------------------------------------
PWD.GetPWUID	>STYA ZPPtr1

				lda #0
				sta (pRWReg)

				>LDYAI PWDX.GetPWUID
				>STYA RAMSW.2AUX
				
				jsr RAMSW.2AUXJSR
				
				pha
				lda A2osX.ActBnk
				sta (pRWReg)
				pla

				rts
*--------------------------------------
MAN
SAVE usr/src/sys/kernel.s.pwd
LOAD usr/src/sys/kernel.s
ASM
