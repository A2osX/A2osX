NEW
  AUTO 3,1
*--------------------------------------
F.Decl			ldy #SYM.Cnt
				lda #$ff
				sta (ZP.pLSymBuf),y		func() : unknown arg count

				jsr CORE.GetNCharNBNL	Skip (
				bcs .9

				cmp #')'				func()
				bne F.Decl.Args

				jsr CORE.GetNCharNBNL	Skip ')'
				bcs .9

				cmp #';'
				bne .9

				jmp SYM.Update			Store this declaration & exit

.9				lda #E.CSYN
				sec
.99				rts
*--------------------------------------
F.Decl.Args		bit bPass2
				bmi .10

				jsr SCOPE.New			function (type.....
				bcs .99

				lda ScopePtr
				clc
				adc #SCOPE.State
				tay
				lda #$FF				add args
				sta (ScopeStk),y

				jsr SYM.AddScopeID
				bra .11

.10				jsr SYM.GetScopeID

				jsr SCOPE.Get

.11				jsr SYM.IncCnt			$FF -> 0

				jsr TYPE.IsVoid
				beq .1					void function(....

				lda #'R'
				jsr SYM.NewLabelA
				bcs .99
*--------------------------------------
.1				jsr F.Decl.GetType		function (type.... ?
				bcs .99

				jsr TYPE.IsVoid
				beq .5					function (void)

				stz	R4+1				pass1:Param Size

				ldy #SYM.fParamSize
				lda (ZP.pLSymBuf),y
				sta R4					pass2:Param Ptr

.2				jsr F.Decl.NewArg
				bcs .99

				jsr SYM.IncCnt

				lda (ZP.pTypeBuf)
				cmp #SYM.T.VARIADIC
				beq .5					function ( , ...)

				jsr CC.GetCharNB
				bcs .9

				cmp #')'
				beq .6

				cmp #','
				bne .9

				jsr CORE.GetNCharNBNL	Skip ,
				bcs .9

				jsr F.Decl.GetType
				bcs .99

				jsr TYPE.IsVoid
				bne .2

.9				lda #E.CSYN
				sec
.99				rts
*--------------------------------------
.5				jsr CC.GetCharNB
				bcs .9

				cmp #')'
				bne .9

.6				bit bPass2
				bmi .60

				lda R4+1
				ldy #SYM.fParamSize
				sta (ZP.pLSymBuf),y

.60				jsr CORE.GetNCharNBNL	Skip ')'
				bcs .9

				cmp #';'
				bne .7

				jsr SCOPE.Close			function ();
				bcs .99

				jmp SYM.Update			Store this declaration & exit
*--------------------------------------
.7				cmp #'{'
				bne .9
*--------------------------------------
F.Def			>LDA.G CC.bInitCode
				bmi .1

*			jsr CODE.DEBUG

				lda #$60				RTS
				jsr CODE.EmitByte		Close INIT code
				bcs .99

				lda #$FF
				>STA.G CC.bInitCode
*--------------------------------------
.1				jsr SYM.SetAddrCCode	f() Code starts HERE

				jsr SYM.Update			Store f() Declaration
				bcs .99

				lda ScopePtr
				clc
				adc #SCOPE.State
				tay
				lda #0
				sta (ScopeStk),y

				lda #$ff
				>STA.G CC.CmdSave		for STMT.New00

				jsr STMT.New00
				bcs .99

				jsr STMT.SetTypeCP		type = {}
				bcs .99

				>LDYA L.PCC.BS			pBase setup
				jsr CODE.EmitPCC
				bcs .99

				ldy #SYM.Cnt
				lda (ZP.pLSymBuf),y
				beq .5					f(void)

				cmp #1
				bne .5					f(arg1, ....

				ldy #SYM.fParamSize
				lda (ZP.pLSymBuf),y

				cmp #2
				bne .2

				>LDYA L.PCC.STOREYA
				jsr CODE.EmitPCC
				bcs .99

				bra .5

.2				bcs .5					> 2, already stacked

				>LDYA L.PCC.STOREA
				jsr CODE.EmitPCC
				bcs .99

.5				lda #'F'				Get FRAME size
				jsr SYM.LookupLabelA
				bcs .99

				txa

				jmp CODE.AddLocal		Create room on stack for locals

.99				rts
*--------------------------------------
* ZP.pLSymBuf = f() declaration
* ZP.pTypeBuf = arg type
* update SCOPE.LocalPtr, SCOPE.FrameSize
*--------------------------------------
F.Decl.NewArg	ldy #SYM.Cnt
				lda (ZP.pLSymBuf),y
				ora #$30
				>STA.G CC.Label+1

				>LDYA.G CC.LSymID
				>STYA.G CC.LSymIDSave

				>LEA.G CC.ArgBuf
				>STYA ZP.pLSymBuf

				jsr CC.GetCharNB
				bcs .9

				jsr CC.IsLetter			function (type name... ?
				bcs .6

				bit bPass2
				bmi .1

				jsr SYM.NewArg			create named arg....
				bcs .99

				jsr SYM.Store
				bcs .99

*				clc						pass 1:update ParamSize
				lda R4+1
				ldy	#SYM.SizeOf
				adc (ZP.pLSymBuf),y
				sta R4+1
				bra .4
*--------------------------------------
.1				jsr SYM.NewArg
				bcs .99

				sec						pass 2:update ParamPtr
				lda R4
				ldy	#SYM.SizeOf
				sbc (ZP.pLSymBuf),y
				sta R4

				ldy #SYM.Addr
				sta (ZP.pLSymBuf),y

				jsr SYM.Update			pass 2:and relative address
				bcs .99
*--------------------------------------
.4				ldx ZP.pLineBuf
				phx
				ldx ZP.pLineBuf+1
				phx

				>LEA.G CC.Label
				>STYA ZP.pLineBuf

				bit bPass2
				bmi .5

				jsr SYM.New				...duplicate as "_0,_1,..."
				bcs .80

				jsr SYM.Store
				bra .80
*--------------------------------------
.5				jsr SYM.Get
				bcs .80

				lda R4
				ldy #SYM.Addr
				sta (ZP.pLSymBuf),y

				jsr SYM.Update
				bra .80
*--------------------------------------
.9				lda #E.CSYN
				sec

.99				rts
*--------------------------------------
.6				ldx ZP.pLineBuf
				phx
				ldx ZP.pLineBuf+1
				phx

				>LEA.G CC.Label
				>STYA ZP.pLineBuf

				jsr SYM.NewArg			create "_0,_1,..."
				bcs .80

				jsr SYM.Store

.80				plx
				stx ZP.pLineBuf+1
				plx
				stx ZP.pLineBuf
				bcs .99

				>LDYA.G CC.pLSymBuf
				>STYA ZP.pLSymBuf

				>LDYA.G CC.LSymIDSave
				>STYA.G CC.LSymID

				clc
				rts
*--------------------------------------
F.Decl.GetType	lda (ZP.pLineBuf)
				cmp #'.'
				bne .5

				ldx #2

.1				jsr CC.GetNextChar
				bcs .9

				cmp #'.'
				bne .9

				dex
				bne .1

				jsr CORE.GetNCharNB
				bcs .9

				eor #')'
				bne .9

				lda #SYM.T.VARIADIC
				sta (ZP.pTypeBuf)

*				clc
				rts
*--------------------------------------
.5				jsr DECL.GetType
				bcs .9

				rts

.9				lda #E.CSYN
				sec
				rts
*--------------------------------------
F.Def.END		lda #'X'				define EXIT Label
				jsr SYM.NewLabelA
				bcs .99

				lda #'F'				define FRAME size
				jsr SYM.NewLabelA
				bcs .99

				lda ScopePtr
*				clc
				adc	#SCOPE.FrameSize
				tay
				lda (ScopeStk),y

				jsr CODE.RemLocal
				bcs .99

				jsr STMT.Close
				bcs .99

				jsr SCOPE.Close			function () {};
				bcs .99

*			jsr CODE.DEBUG

				>LDYA L.PCC.BR
				jsr CODE.EmitPCC
				bcs .99

				lda #$60				RTS
				jmp CODE.EmitByte

.99				rts
*--------------------------------------
* in :
*  f() in ZP.pRSymBuf
*  Y,A = pLSymBuf
*--------------------------------------
F.CallNoRetV	ldy #SYM-1

.1				lda (ZP.pLSymBuf),y
				sta (ZP.pRSymBuf),y
				dey
				bpl .1

				>LDYA ZP.pRSymBuf

F.CallRetV		ldx ZPPtr1				local : Variadic size
				phx
				ldx ZPPtr1+1			local : StackInit
				phx

				ldx ZPPtr2				local : target pLSym
				phx
				ldx ZPPtr2+1
				phx

				>STYA ZPPtr2

				stz ZPPtr1				Reset Variadic byte count
				stz ZPPtr1+1			Reset StackInit Status

				jsr CORE.GetNCharNB		skip '('
				bcs .90

				ldy #SYM.Cnt
				lda (ZP.pRSymBuf),y
				bpl .1

				lda #E.FUNDEF			should be at least f(void)
				bra .98

.1				jsr F.Call.SetRetV		Add room for RET value if required
				bcs .99

				jsr F.Call.GetArg0		get First Arg in pLSymBuf
				bcs .7

				jsr F.StackSave
				bcs .99

*			jsr CODE.DEBUG

.3				jsr SYM.NextRBuf

*			>DEBUG

				>LDYA ZP.pLSymBuf
				jsr EXP.Eval
				bcs .99

				jsr F.StackPushV
				bcs .99

				jsr F.Call.NextArg

				jsr SYM.PrevRBuf
				jsr SYM.PrevLBuf

				jsr CC.GetCharNB
				bcs .90

				cmp #','
				bne .6

				jsr CORE.GetNCharNBNL	Skip ','
				bcs .90

				jsr F.Call.GetArg
				bcc .3					Another ARG....

				bra .99					extra args....error

.6				cmp #')'
				bne .90

				jsr F.Call.GetArg
				bcs .8					no more arg after ')', exit

				lda (ZP.pLSymBuf)		#SYM.T
				cmp #SYM.T.VARIADIC
				bne .90					missing arg

				jsr SYM.PrevLBuf

				lda ZPPtr1				push VARIADIC byte count
				jsr CODE.PUSHI

.7				jsr CC.GetCharNB
				bcs .90

				cmp #')'
				bne .90

.8				jsr CORE.GetNCharNB		Skip )

				jsr F.Call2
				bcs .99

				jsr F.StackRestore
				bcs .99

				jsr F.Call.SetRetState

				clc

				bra .99

.90				lda #E.CSYN

.98				sec

.99				plx
				stx ZPPtr2+1
				plx
				stx ZPPtr2

				plx
				stx ZPPtr1+1
				plx
				stx ZPPtr1

				rts
*--------------------------------------
F.Call2			ldy #SYM.Addr+1

				lda (ZP.pRSymBuf),y
				beq .4

				pha

				dey
				lda (ZP.pRSymBuf),y
				tax

				ply						Y=HI,X=LO

				cpy #1
				bne .1

				jsr CODE.LIBC
				bcc .8

				rts
*--------------------------------------
.1				cpy #3+LIBS.MAX
				bcs .3

				jsr CODE.LIBCALL
				bcc .8

				rts
*--------------------------------------
.3				lda #$EA				NOP
				jsr CODE.EmitByte
				bcs .99

				lda #$EA				NOP
				jsr CODE.EmitByte
				bcs .99

				lda #$20				JSR abs
				jsr CODE.TOABSYX
				bcc .8

				rts
*--------------------------------------
.4				bit bPass2
				bmi .91

				lda #5					leave room for LDX #imm, JSR abs
				clc
				adc ZPCCCode
				sta ZPCCCode
				bcc .8

				inc ZPCCCode+1

.8				lda ZPPtr2
				ora ZPPtr2+1
				bne .80

				ldy #SYM.SizeOf
				lda (ZP.pRSymBuf),y

				tax
				beq .80					void

				cmp #3
				bcc .80

.81				jsr CODE.INCPSTACK
				dex
				bne .81

.80				clc
				rts

.91				lda #E.FUNDEF
				sec
.99				rts
*--------------------------------------
F.Call.GetArg0	lda #$30				arg "_0, _1, ...."
				>STA.G CC.Label+1

F.Call.GetArg	.DO _DBG_SYM=1
				jsr DEBUG.LBLL1
				.FIN
				>SS
				>PUSHW.G CC.pSyms
				>PUSHEA.G CC.Label

				ldy #SYM.ScopeID+1
				lda (ZP.pRSymBuf),y
				>PUSHA
				dey
				lda (ZP.pRSymBuf),y
				>PUSHA

				>PUSHWI	0				NextPtr
				>KAPI SListLookup
				>SR
				bcs .9

				>STYA ZP.pLookup
				jsr SYM.NextLBuf

				>SS
				>PUSHW.G CC.pSyms
				>PUSHW ZP.pLookup
				>PUSHW ZP.pLSymBuf
				>PUSHWI 65535			all
				>PUSHWZ					From Start
				>KAPI SListGetData
				>SR

.9				.DO _DBG_SYM=1
				jsr DEBUG.LBLL2
				.FIN

				rts
*--------------------------------------
F.Call.NextArg	lda (ZP.pLSymBuf)		#SYM.T
				cmp #SYM.T.VARIADIC
				bne .1					if VARIADIC, don't advance to next arg

				jsr EXP.GetRSymSizeOf

				clc						make sure pointer only 2 bytes
				adc ZPPtr1
				sta ZPPtr1				Add to byte count
				rts

.1				>INC.G CC.Label+1
				rts
*--------------------------------------
F.Call.SetRetV	ldy #SYM.SizeOf
				lda (ZP.pRSymBuf),y
				cmp #2
				bcs .1

*				clc

				rts

.1				bne .2

				clc
				rts

.2				cmp #4
				bne .3

				>LDYA L.PCC.SL
				jmp CODE.EmitPCC

.3				cmp #5
				bne *

				>LDYA L.PCC.SF
				jmp CODE.EmitPCC
*--------------------------------------
F.Call.SetRetState
				ldy #SYM.SizeOf
				lda (ZPPtr2),y
				cmp #2
				bcs .1

				lda #State.RVinA
				sta State

*				clc

				rts

.1				bne .2

				lda #State.RVinYA
				sta State

				clc
				rts

.2				cmp #4
				bne .3

				lda #State.RVonStack
				sta State

				clc
				rts

.3				cmp #5
				bne *

				lda #State.RVonStack
				sta State

				clc
				rts
*--------------------------------------
F.StackSave		ldy #SYM.Cnt
				lda (ZP.pRSymBuf),y
				cmp #2
				bcs .3					more than 1 arg

				ldy #SYM.SizeOf
				lda (ZP.pLSymBuf),y		First Arg
				cmp #3
				bcc .9					only 1 or 2 bytes in Y,A

.3				dec ZPPtr1+1			for F.StackPushV & F.StackRestore

				>LDYA L.PCC.SS
				jmp CODE.EmitPCC

.9				rts
*--------------------------------------
F.StackPushV	bit ZPPtr1+1
				bpl .8

				lda State
			beq *

*				and #State.RVonStack
*				bne .8
			bmi .8

				bit #State.RVinA
				beq .6

				jmp CODE.PUSHA

.6				bit #State.RVinYA
				beq *

				jmp CODE.PUSHYA

.7				bra *


.8				clc
				rts
*--------------------------------------
F.StackRestore	bit ZPPtr1+1
				bpl .8

				>LDYA L.PCC.SR
				jmp CODE.EmitPCC

.8				clc
				rts
*--------------------------------------
MAN
SAVE usr/src/bin/cc.s.f
LOAD usr/src/bin/cc.s
ASM
