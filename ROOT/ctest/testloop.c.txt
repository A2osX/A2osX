NEW
  AUTO 3,1
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *pBuf = malloc(256);

int main(int argc, char *argv[]) {

    for (int i=0; i<=argc; i++) printf("argv[%D]=%s\r\n", i, argv[i]);
    getchar();
    strcpy(pBuf, argv[0]);
    strcat(pBuf, ".c");
    printf("Filename=%s\r\n", pBuf);

    FILE* pFile = fopen(pBuf, O_RDONLY, 0, 0);
    printf("pFile=%H\r\n", pFile);
    int linecnt=0;

    while (NULL != fgets(pBuf, 255, pFile)) {
        linecnt++;
        printf("%5D:%s\r\n", linecnt, pBuf);
        }
    fclose(pFile);
    getchar();

    free(pBuf);
    puts("All done.");
    }
MAN
TEXT root/ctest/testloop.c
